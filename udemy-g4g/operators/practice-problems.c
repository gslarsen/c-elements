/**
 * @file practice-problems.c
 * @author Gregory Larsen
 * @date 2025-05-22
 */

#include <stdio.h>

int main(int argc, char *argv[]) {
  int x = 10, y = 20, z = 5, a;
  // a = (x < y < z);
  printf("a = %d\n", a);

  return 0;
}